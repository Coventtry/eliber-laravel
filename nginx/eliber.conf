server {
    listen 80;
    server_name eliber.tuinstitucion.edu.ar;
    root /var/www/eliber/public;
    index index.php;

    charset utf-8;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# HTTPS — descomenta después de correr certbot:
# server {
#     listen 443 ssl;
#     server_name eliber.tuinstitucion.edu.ar;
#     root /var/www/eliber/public;
#     index index.php;
#     ssl_certificate     /etc/letsencrypt/live/eliber.tuinstitucion.edu.ar/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/eliber.tuinstitucion.edu.ar/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     charset utf-8;
#     location / { try_files $uri $uri/ /index.php?$query_string; }
#     location ~ \.php$ {
#         fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         include fastcgi_params;
#     }
#     location ~ /\.(?!well-known).* { deny all; }
# }
